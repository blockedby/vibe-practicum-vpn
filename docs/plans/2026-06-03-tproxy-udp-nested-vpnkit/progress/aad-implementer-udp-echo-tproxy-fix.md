# Progress: UDP echo TPROXY blocker fix

- 2026-06-03: Started aad-implementer task. Read AGENTS.md; CLAUDE.md absent. `git status --short` was clean before edits. Inspected plan, prior matching-bundle verification, tproxy sing-box template, routing/entrypoint scripts, Docker setup, template test, and inner non-DNS UDP failure classification.
- 2026-06-03: Plan: use an isolated reduced UDP echo harness before nested OpenVPN; test non-DNS UDP entering outer OpenVPN/TProxy and reaching a local echo target; make only the minimal confirmed fix while preserving default redirect mode.
- 2026-06-03: Preparing RED reduced echo harness with isolated local Docker resources. The harness will render the current tproxy template with `selected-native-out` as a block outbound, place a UDP echo target on the vpnkit-side Docker network, connect an OpenVPN client from an isolated client container, and prove non-DNS UDP to the echo target fails before the private-target direct routing fix.
- 2026-06-03: First RED harness startup failed before OpenVPN readiness because the temp compose omitted the sing-box deprecated-DNS env flags already present in repo compose. No production containers were touched; isolated resources were removed. Updated the temp harness to mirror those env flags and rerunning RED.
- 2026-06-03: RED harness second startup proved the temp `selected-native-out=block` fixture also blocked DNS used for remote rule-set startup. Adjusted only the temp fixture DNS detours to `direct-out` so the reduced check isolates the non-DNS UDP route behavior.
- 2026-06-03: RED reduced echo reproduced after gating client start on routing completion: OpenVPN client route to private echo target used `tun0`, generic UDP TPROXY counter incremented, echo target did not receive, client timed out.
- 2026-06-03: Tested and rejected narrower sing-box-only hypotheses: private `direct-out` rule, `udp_connect`/`udp_timeout`, explicit fwmark, `ip_nonlocal_bind`, and post-TPROXY fallback. Confirmed TPROXY is terminal in this backend and pre-TPROXY private UDP forwarding/MASQUERADE restores egress.
- 2026-06-03: Implemented tproxy-mode-only private UDP bypass in `docker/vpnkit/setup-routing.sh` plus dry-run regression test `tests/vpnkit-setup-routing-test.sh`; redirect mode remains unchanged.
- 2026-06-03: GREEN reduced echo passed with source implementation only using project `udp_echo_fix_impl`, port `21404/udp`, network `udp_echo_fix_impl_net`; echo server received the probe and private UDP bypass/NAT/FORWARD counters incremented.
- 2026-06-03: Feasible isolated local nested rerun passed using project `udp_echo_fix_nested`, outer port `21405/udp`, network `udp_echo_fix_nested_net`; outer tunnel came up, route to inner private endpoint used `tun0`, inner `tun1` came up on temp subnet `10.90.0.0/24`, and inner server accepted the client.
- 2026-06-03: Targeted checks passed: `bash tests/vpnkit-setup-routing-test.sh`, `bash tests/vpnkit-singbox-template-test.sh`, `bash -n docker/vpnkit/*.sh scripts/*.sh tests/*.sh`, and `sing-box check` on a temp rendered tproxy config (deprecation warnings only). Cleanup check found no `udp-echo-fix*` containers or `udp_echo_fix*` networks.
