# Progress: aad-implementer TUN canary validation

- 2026-06-03: Entered optional Task 6 only after Task 5 checks passed. Safe prereq check (without printing values) showed Docker available and existing gitignored rendered OpenVPN/client configs present.
- 2026-06-03: Full `scripts/vpnkit-render-local-configs.sh` was blocked by missing gitignored source PKI files under `secrets/vps/openvpn/pki/`; existing rendered configs were present. Created only gitignored `secrets/vps/rendered/sing-box/config.tun.json` from existing rendered selected outbound without printing contents; `sing-box check` passed with deprecation env flags.
- 2026-06-03: Isolated local Docker lab `vpnkit_tun_canary_lab_21510` on `21510/udp` passed TUN readiness, baseline OpenVPN connect, UDP DNS `NOERROR`, HTTPS hostname `200`, and literal-IP HTTPS `200`. TUN interface counters incremented and redirect/tproxy capture chains were absent in tun mode.
- 2026-06-03: Cleanup completed via `docker compose -p vpnkit_tun_canary_lab_21510 down -v --remove-orphans`; no matching containers/networks remained. No live-host or production container mutation attempted.
- 2026-06-03 continuation: Started live/staged Task 6 validation continuation after local baseline. Workspace was clean before editing; reading task package and repo guidance before any live mutation.
- 2026-06-03 continuation: `config/private-endpoints.local.env` is readable and was sourced without printing values. Direct SSH aliases `vibe-practicum` and `moscow-tiger` are reachable; the env-provided VPS placeholder alias was not used. Required gitignored rendered OpenVPN/sing-box TUN/client files are present locally.
- 2026-06-03 continuation: Captured safe before metadata for production `vpnkit` and `current-vpnkit-1` (status/restart/start time only). Env public endpoint variable was placeholder, but SSH alias host target has usable syntax and will be used internally without printing for temp profile rewrites.
- 2026-06-03 continuation: Live wrapper created isolated TUN server `vpnkit_tun_live_21631`, inner server `vpnkit_tun_inner_21632`, network `vpnkit_tun_net_21631_21632`, and moscow echo container on `21633/udp`. Local client reached outer tunnel; HTTPS/literal-IP passed; public UDP echo timed out via `tun0`; nested inner attempt timed out. Wrapper then stopped on a shell quoting bug before moscow stage/cleanup, so continuing against the same fresh isolated run resources and will cleanup afterward.
- 2026-06-03 operator continuation: Reprobed public UDP echo through the still-running isolated TUN server using a Python UDP client in a disposable local OpenVPN client container. The route to the public echo endpoint used `tun0`, the echo endpoint received a datagram from the isolated server public IP, and the client received the echo response. This converts the TUN public non-DNS UDP result to PASS for the local-client/vibe-server/public-echo topology; earlier timeout was a harness artifact.
