# aad-implementer TUN validation follow-up progress

- 2026-06-03: Started follow-up in worktree `vpnkit-tproxy-udp-nested`; read AGENTS/task/matrix/report/previous evidence and confirmed required checks from prompt/task file. `git status --short` was clean before edits. `config/private-endpoints.local.env` is present and will be sourced without printing values before live operations.
- 2026-06-03: Implementation plan: (1) exact moscow/vibe production metadata + known-leftover cleanup checks; (2) use fresh high ports/names for isolated vibe TUN server, optional inner server, local/moscow disposable clients and echo; (3) capture sanitized Stage C/D and if needed decisive Stage B evidence; (4) exact cleanup/final leftover checks; (5) update matrix/report/implementer report; (6) run required repo checks; (7) commit safe docs only and push only if full criteria achieved.
- 2026-06-03: Env-file preflight found the VPS SSH variable resolves to the sanitized example placeholder rather than a usable alias; no live mutation occurred. Continuing with the explicit host aliases from the delegated prompt (`vibe-practicum`, `moscow-tiger`) without printing private endpoint values.
- 2026-06-03: Preflight exact known cleanup/status retry completed with direct aliases: both production containers `running`/restart `0` with unchanged start times; known old moscow/vibe leftovers reported zero by exact-name/path checks. Starting fresh isolated run with ports `21881/udp`, `21882/udp`, `21883/udp`.
- 2026-06-03 manual continuation: Subagent stalled before creating fresh resources. Main assistant continued manually with fresh final resources `vpnkit_tun_final_21941_*` on ports `21941/udp` and `21942/udp`.
- 2026-06-03 manual continuation: Local Stage A passed: outer `tun0` up, routes to public NTP and inner endpoint via outer `tun0`, UDP DNS `NOERROR`, HTTPS/literal `200`, public non-DNS UDP NTP response received, `sb-tun0` route/counters present, redirect/tproxy capture chains absent.
- 2026-06-03 manual continuation: Local Stage B passed after using temp-only inner subnet `10.90.0.0/24`, inner client `dev tun1`, and route-pull suppression. Inner server accepted the client and inner `tun1` appeared with `10.90.0.2/24`.
- 2026-06-03 manual continuation: `moscow-tiger` SSH was intermittent but eventually reachable. Stage C passed from a disposable moscow client: outer `tun0`, UDP DNS `NOERROR`, HTTPS/literal `200`, route to public NTP via outer `tun0`, public UDP NTP response received.
- 2026-06-03 manual continuation: Stage D passed from the same disposable moscow client: route to inner endpoint via outer `tun0`, inner server accepted the client, and inner `tun1` appeared with `10.90.0.2/24`.
- 2026-06-03 manual continuation: Exact cleanup completed on local host, `vibe-practicum`, and `moscow-tiger`; final leftover checks for prefix `vpnkit_tun_final_21941` and ports `21941/21942/21943` found no matching containers/networks. Production `vpnkit` and `current-vpnkit-1` stayed running with restart count 0 and unchanged start times.
