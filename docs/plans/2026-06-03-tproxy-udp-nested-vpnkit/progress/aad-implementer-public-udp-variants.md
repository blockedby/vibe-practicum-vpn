# aad-implementer progress: public UDP variants

- 2026-06-03: Started Task 4 in provided worktree. Read AGENTS, task package plan/matrix/slice-owner report, implementation-report/task-package/devops-readiness skills. `git status --short` was clean before edits. Confirmed required verification commands from prompt: `bash tests/vpnkit-setup-routing-test.sh`, `bash tests/vpnkit-singbox-template-test.sh`, `bash -n docker/vpnkit/*.sh scripts/*.sh tests/*.sh`, `go test ./...`, `git diff --check`.
- 2026-06-03: Confirmed gitignored private endpoint env and matching OpenVPN/sing-box bundle files are readable without printing values. Next: run isolated live variant harness with sanitized summaries only.
- 2026-06-03: First harness start stopped before live mutation because the public endpoint variable was placeholder/unusable. Adjusted temp-only harness to use the SSH alias HostName as a private temp profile value without printing it, matching prior task-package usage of SSH aliases.
- 2026-06-03: First full feasible-variant pass exposed a temp harness client env bug; probes reached OpenVPN but did not send echo packets. Cleaned root-owned remote temp paths with non-interactive sudo. Fixed temp harness env passing and cleanup; rerunning with fresh ports/resources.
- 2026-06-03: Same-host echo run proved why that topology is invalid for this criterion: the client route to the VPN server's own endpoint stays on the OpenVPN transport interface rather than `tun0`. Reworked temp harness to keep vpnkit server on vibe-practicum and use a separate isolated UDP echo container on the secondary host as server-only (not client), with safe production metadata for both hosts and no production container mutation.
- 2026-06-03: Separate-host live run completed with fresh resources: variants 1-4 routed to public echo target via `tun0` and timed out while generic public UDP TPROXY counters incremented 1 packet / 51 bytes and private bypass stayed zero. Variant 5 nft canary hit nft syntax/setup failures in two temp attempts; Variant 6 marked infeasible due current tun-mode wiring; Variant 7 port-bypass comparison routed via `tun0` and timed out with generic TPROXY counter zero. Cleaned isolated server/temp resources and followed up to remove retained echo containers from the echo host. Removed local temp profiles/logs/tarballs.
- 2026-06-03: Required checks passed: `bash tests/vpnkit-setup-routing-test.sh`, `bash tests/vpnkit-singbox-template-test.sh`, `bash -n docker/vpnkit/*.sh scripts/*.sh tests/*.sh`, `go test ./...`, `git diff --check`. Wrote final matrix/report. Preparing commit.
