# aad-implementer progress: live validation after private UDP bypass

- 2026-06-03: Started delegated Task 3 in worktree `vpnkit-tproxy-udp-nested`; read AGENTS/README/DOCKER_SETUP/plan and prior sanitized evidence. `git status --short` showed an existing in-scope dirty `plan.md` continuation section from owner context; proceeding without overwriting it.
- 2026-06-03: Implementation plan: use fresh isolated names/ports/temp paths; source private endpoint env only if readable without printing values; validate Stage 1/Stage 2 baseline, feasible matching-bundle nested private OpenVPN, reduced public UDP echo, production safe metadata, and cleanup.
- 2026-06-03: Private endpoint env is readable and required gitignored matching-bundle paths are present by path/count only. Preparing fresh live validation resources on ports `21470/udp`, `21471/udp`, and `21472/udp`; no endpoint/profile contents printed.
- 2026-06-03: First live-validation wrapper aborted before remote mutation due to a local shell indirect-expansion bug in endpoint resolution; no isolated Docker/process resources had been created yet. Retrying with the local wrapper fixed.
- 2026-06-03: Second wrapper attempt also aborted locally before remote mutation on endpoint-resolution syntax. Simplifying to explicit variable fallback and retrying; no live resources were created before the abort.
- 2026-06-03: First actual live wrapper reached preflight and started the isolated public echo process, then failed before vpnkit server startup due to a temp-tree copy error (`cp -a . outer`). Wrapper trap ran cleanup. Verifying no leftovers before retry.
- 2026-06-03: Full live isolated run on fresh ports `21470/udp` + `21471/udp` passed Stage 1 baseline, Stage 1 inner nested, Stage 2 baseline, and Stage 2 inner nested. Public UDP echo in that run timed out, but it was attempted after inner tunnel establishment, so I am running a separate reduced public echo check with fresh resources before any inner tunnel changes routes.
- 2026-06-03: Separate reduced public UDP echo check on `21473/udp` + `21475/udp` proved route via outer `tun0`, timed out, and incremented the generic public UDP TPROXY rule (`1` packet); cleanup passed. Running one fresh same-host nested check with the client isolated from the inner Docker network because the earlier same-host nested `tun1` was not a route-to-inner-through-outer proof.
- 2026-06-03: Focused same-host nested wrapper started fresh isolated servers but failed before client run due to a relative path bug while preparing temp client profiles; wrapper cleanup ran. Verifying no leftovers before deciding whether to retry.
- 2026-06-03: Focused same-host true nested retry on `21478/udp` + `21479/udp` passed baseline, route-to-inner via `tun0`, inner `tun1`, private UDP bypass counters, and cleanup.
- 2026-06-03: Wrote sanitized verification artifact and implementation report. Running `git diff --check` before commit.
- 2026-06-03: `git diff --check` passed. Preparing scoped commit for new report/progress/verification artifacts only; leaving the pre-existing owner `plan.md` dirty section unstaged.
- 2026-06-03: Committed live validation artifacts as `262eb8a docs: record live tproxy udp validation`. Updating report commit metadata.
- 2026-06-03: Added `478617e docs: finalize live validation report` to report commit metadata; making metadata-only follow-up commit.
