# Public-safe local vpnkit asset-rendering inputs.
# Keep the generated PKI, inline profile, rendered server config, and any
# subscription URL under the gitignored secrets/vpnkit-local/ tree.
#
# These values are also the safe sourced-config contract for an explicit
# local-kde-host acceptance. Do not add helper/test seams, alternate resource
# roots, or host-routing overrides here; live acceptance rejects them before
# Docker/NetworkManager mutation.
VPNKIT_LOCAL_SECRETS_DIR=secrets/vpnkit-local
VPNKIT_LOCAL_ENDPOINT=127.0.0.1
VPNKIT_LOCAL_OPENVPN_PORT=21194
VPNKIT_LOCAL_OPENVPN_PUSH_DNS=8.8.8.8
VPNKIT_LOCAL_COMPOSE_PROJECT=vpnkit-local
VPNKIT_LOCAL_DOCKER_SUBNET=172.30.89.0/24
VPNKIT_LOCAL_CONTAINER_ADDRESS=172.30.89.2
# VERSION=2 host underlay destination protection is installed at 998/999,
# before the source-underlay pair at 1000/1001. New installs and v1 migrations
# persist VERSION=2 with these values; VERSION=1 configs remain source-only.
VPNKIT_LOCAL_DESTINATION_RULE_PRIORITY=998
VPNKIT_LOCAL_DESTINATION_FAIL_CLOSED_PRIORITY=999
VPNKIT_LOCAL_RULE_PRIORITY=1000
VPNKIT_LOCAL_FAIL_CLOSED_PRIORITY=1001
VPNKIT_LOCAL_POLICY=strict
VPNKIT_BOOTSTRAP_PICK_ON_START=true
VPNKIT_BOOTSTRAP_MAX_NODES=50
# First selection can take roughly candidate_count * timeout_seconds.
VPNKIT_LOCAL_BOOT_TIMEOUT_SECONDS=1200
VPNKIT_LOCAL_RETEST_TIMEOUT_SECONDS=180
# Mutating lifecycle commands share a private descriptor lock and retain a
# durable journal under secrets/vpnkit-local/state.
VPNKIT_LOCAL_LIFECYCLE_LOCK_WAIT_SECONDS=5
VPNKIT_LOCAL_LIFECYCLE_COMPENSATION_TIMEOUT_SECONDS=30
# One-button TUI start/stop manages only the `vpnkit-local` NM connection.
# The helper waits only this long for the owned UUID's IP-to-tun proof.
VPNKIT_LOCAL_NM_CONNECT_TIMEOUT_SECONDS=30
# Set false for container-only diagnostics before the one-time host helper install.
VPNKIT_LOCAL_MANAGE_NETWORKMANAGER=true
